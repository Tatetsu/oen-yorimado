# TODO: やるべきことリスト

## 凡例
- [ ] 未着手
- [x] 完了
- [~] 一部実装済み / 要確認

---

## Phase 1: 初期構築・基本集計（F-01〜F-04）

- [x] F-01: シート初期セットアップ（setup.gs）
- [x] F-02: 月別集計更新（monthly-summary.gs）
- [x] F-03: 確定来館記録生成（confirmed-visits.gs）
- [x] F-04: 児童別ビュー更新（child-view.gs）
- [x] 来館カレンダービュー導入（visit-calendar.gs）— 確定来館記録のマトリクス表示化

## Phase 2: 余りポイント振り分け（F-05〜F-06）

- [x] F-05: 余りポイント自動振り分け — 月初1日トリガー（allocation.gs）
- [x] F-06: 振り分け手動実行 — カスタムメニューから実行
- [x] 振り分け記録の補完データ永続化（スタッフ名・入退所時間・体温・生活記録等）
- [x] 月別集計で振り分け分を来館数に加算する対応（二重計算に注意）
- [ ] 定型文マスタシート導入（振り分け時「その他連絡事項」のフォールバック用）
  - 「定型文マスタ」シートに定型文を登録
  - 振り分け時: 自児童ノート → 他児童ノート → 定型文マスタからランダム → 「特になし」の優先順

## Phase 3: メール通知・月次確定（F-07〜F-08）— 一部完了

- [x] F-07: 保護者向け来館報告メール（毎朝8時トリガー + 手動送信）
- [ ] F-08: 月次データ確定処理（月初・振り分け完了後）

---

## 営業日・祝日対応

現状: 営業日 = 全曜日（祝日除外なし）。要件定義書で「将来ユーザー側で作成・適用予定」と記載。

- [ ] 祝日カレンダーの導入方針を決める
  - 案1: 祝日マスタシートを手動管理（スプレッドシートに祝日一覧を入力）
  - 案2: Google Calendar API で祝日を自動取得
  - 案3: 祝日データを年初にGASで一括生成（ハードコードまたは外部API）
- [ ] 振り分けロジック（allocation.gs）で祝日を除外する処理を追加
- [ ] 来館カレンダー（visit-calendar.gs）で祝日を視覚的に区別（背景色等）
- [ ] 月別集計の営業日数計算に祝日除外を反映
- [ ] 休業日（年末年始・お盆等）の扱いを決める

---

## 設定シート外出し（定数→スプレッドシート管理化）

非エンジニアでも設定変更できるよう、GASコード内の定数をスプレッドシート「設定」シートに移行する。

### 1. utils.gs: SHEET_NAMES追加 + getSettings() + ヘルパー関数作成
- [ ] `SHEET_NAMES` に `SETTINGS: '設定'` を追加
- [ ] 以下の定数・変数を削除
  - `ALLOCATION_DEFAULTS`（70-80行目）
  - `MAX_VISITS_PER_DAY`（83行目）
  - `EMAIL_TEMPLATE`（108-129行目）
- [ ] `getSettings()` 関数を新規作成（設定シートA列=キー、B列=値をオブジェクトで返す。グローバル変数でキャッシュ）
- [ ] ヘルパー関数を追加（全てフォールバック値付き）
  - `getMaxVisitsPerDay()` → Number or 7
  - `getAllocationDefaults()` → オブジェクト
  - `getEmailTemplate()` → テンプレート文字列

### 2. allocation.gs: 定数参照を設定シート読み取りに置換（#1完了後）
- [ ] `MAX_VISITS_PER_DAY` → `getMaxVisitsPerDay()` に置換（190行目・216行目）
  - 関数冒頭で `var maxVisitsPerDay = getMaxVisitsPerDay();` としてキャッシュ
- [ ] `ALLOCATION_DEFAULTS.xxx` → `getAllocationDefaults()` に置換（428-450行目、543行目）
  - 関数冒頭で `var defaults = getAllocationDefaults();` としてキャッシュ

### 3. email.gs: テンプレート+通知先を設定シートから取得（#1完了後）
- [ ] 241行目: `EMAIL_TEMPLATE` → `getEmailTemplate()` に置換
- [ ] `getErrorNotifyRecipients_()` 内の `ScriptProperties ERROR_NOTIFY_EMAILS` → 設定シートの「エラー通知先メール」から取得に変更（ScriptPropertiesはフォールバックとして残す）

### 4. setup.gs: setupSettingsSheet_() を新規追加
- [ ] `setupSettingsSheet_(ss)` 関数を作成
  - A列: 設定名、B列: 値、C列: 説明
  - A列・C列はシート保護（編集不可）、B列は薄黄色背景 #FFF9C4
  - 初期データ12項目（1日最大来館数、デフォルト入退所時間・体温・生活記録、デフォルト連絡事項、エラー通知先メール、メール本文テンプレート）
  - 既存シートがある場合はデータ上書きせず、新規項目のみ追記
- [ ] `setupAllSheets()` の先頭に `setupSettingsSheet_(ss)` を追加

### 5. 動作確認 + clasp push（#1〜#4完了後、ユーザー確認必須）
- [ ] 旧定数（ALLOCATION_DEFAULTS / MAX_VISITS_PER_DAY / EMAIL_TEMPLATE）がコード内に残っていないかGrep確認
- [ ] 設定シートが存在しない場合もフォールバックで動作するか確認
- [ ] ユーザー確認後に `clasp push`
- [ ] スプレッドシート上で設定シート作成・B列編集→反映を確認

---

## 運用・改善タスク

- [x] clasp push 後のスプレッドシート側での動作確認手順の整備
- [ ] エラー発生時の通知方法（Logger.log のみだと見逃す可能性）
- [ ] 児童マスタの入所状況変更時の影響範囲確認（振り分け済みデータの扱い）
- [ ] フォーム送信時トリガー（onFormSubmit）の安定性確認（GASトリガーの重複実行対策）
- [x] 来館カレンダービュー導入に伴うarchitecture.mdの更新（確定来館記録→来館カレンダー）

---

## 仕様メモ（2026-03-16 ヒアリング）

### 年間利用枠と振り分けロジック
- 児童1人につき年間180回の利用枠がある
- 180÷12 = 約15回/月 → **月16回をMAX**として振り分ける
- 基本は月16回MAXで入れ、年度末に不足分を追加調整するイメージ
- 若干の変動は手動調整の想定

### シートの年単位切り替え
- シートは年単位で新しく切り替える運用
- 現在の年プルダウン（「全て」「2026年」等）は不要になる
- 過去年に遡って作成することはない

### 開館曜日（営業日）
- 開館している曜日は固定で決まっている
- 振り分けは開館曜日（営業日）のみに行う
- 手動での来館曜日選択時も開館曜日から選ぶ形にする

### スタッフ配置ルール
- 1日の来館枠がMAX（7人）の場合、スタッフ2人が必須
- Googleフォームに「担当スタッフ1」「担当スタッフ2」の枠を追加する
- 7人埋まっている場合にスタッフ1人しか選ばれなかったら、固定メンバーを「担当スタッフ2」に自動設定する

---

## 2026-03-18 打ち合わせ対応（plan/20260318.md）

### 完了済み
- [x] 優先度5を正式追加（1〜5のドロップダウン、setup.gs + main.gs）
- [x] 年間利用枠チェックをallocation.gsに追加（MASTER_COL.ANNUAL_QUOTA: 11列目）

### スプレッドシート側で対応中（コード変更なし）
- [~] 退所後の行き先フラグ（「他施設へ移行 / 完全退所」）を児童マスタに追加
- [~] 退所フラグに応じた請求処理の条件分岐（スプレッドシート集計で対応）
- [~] 入所待ちステータスを児童マスタに追加（自動配分の対象外制御）

### 未実装（仕様確認待ち）
- [ ] 月別利用日数のばらつき制御（月ごとの配分を均一にせず変動させる）
  - 月別配分の上限を固定値ではなく min〜max の範囲で扱う設計が必要
  - 要確認: ばらつきの生成方法（ランダム？ルールベース？）
  - 要確認: 年間180日制約の月別分散ルール（宮本さんより確認依頼あり）
- [ ] 名目枠振り分けロジック（石神さん〜松岡さんグループ）
  - 実利用なしで請求用に月5日程度の「名目枠」を振り分ける
  - 要確認: 確定来館記録への書き方（新データ区分「名目枠」を追加するか）
  - 要確認: 名目枠の月5日は固定か変動か
  - 残り枠をレギュラーグループへ回す処理と連動

### 後日GAS対応（スプレッドシート準備完了後）
- [ ] 来館カレンダー・確定来館記録の生成ロジックに退所フラグ条件分岐を追加
  - 他施設移行の児童は翌月以降の振り分け計上を除外する
- [ ] 利用曜日制限フィールドを振り分けロジックに組み込む
  - 児童マスタに曜日制限列はすでに存在（VISIT_DAYS）
  - 現状は「優先曜日」として使用中。「禁止曜日」としての制御に拡張が必要
- [ ] スタッフマスター再設計・Googleフォームプルダウン自動同期
  - 担当スタッフ紐付けからフォーム用プルダウンリストに再設計
  - 1〜2時間ごとの時間トリガーでフォームプルダウンに自動反映
- [ ] 保護者メールアドレス随時追加フロー整備
  - 入力UI（カスタムメニューまたはサイドバー）の実装方法を検討

---

## 設計・ドキュメント整備

- [x] requirements.md の確定来館記録セクションを来館カレンダーに更新
- [x] architecture.md のファイル構成・データフローを現状に合わせて更新
- [ ] 各プランファイルの完了ステータスを記録
