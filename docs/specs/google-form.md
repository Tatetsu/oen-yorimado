# Googleフォーム 設計書

## 概要

スタッフが来館記録を入力するためのGoogleフォーム。回答データはスタッフ用_回答シート（別スプレッドシート）に蓄積され、メインシートにIMPORTRANGEで参照される。

---

## フォームの質問項目

2026-04 の仕様変更で、食事と服薬は行政様式(実績記録票)に合わせて分離されている。さらに 2026-04（後半）の仕様変更で、旧「睡眠」(○/×) を **入眠時刻 / 朝4時チェック / 起床時刻** の3項目（いずれもプルダウン・必須）に置換した。加えて 2026-04（後半）で **「記録日」項目を廃止**（タイムスタンプの送信日時を「利用日」として扱う）。

| 順番 | 質問タイトル | 種別 | 必須 | 選択肢・検証 |
|------|------------|------|------|------------|
| 1 | スタッフ1 | プルダウン | ○ | スタッフマスタ B列から自動同期 |
| 2 | スタッフ2 | プルダウン | × | スタッフマスタ B列から自動同期 |
| 3 | 児童名 | プルダウン | ○ | 児童マスタ B列（稼働・休止のみ）から自動同期 |
| 4 | 入所日時 | 日時 | ○ | 手入力（入所日+時刻） |
| 5 | 退所予定日時 | 日時 | ○ | 手入力（退所日+時刻） |
| 6 | 体温 | 短答 | × | 数値（例: 36.5） |
| 7 | 夕食 | プルダウン | × | ○ / × / △ / 完食 / 半分 / - |
| 8 | 朝食 | プルダウン | × | ○ / × / △ / 完食 / 半分 / - |
| 9 | 昼食 | プルダウン | × | - / ○ / × / △ / 完食 / 半分（他サービス併給時は「-」） |
| 10 | 入浴 | プルダウン | × | ○ / × / △ / - |
| 11 | 入眠時刻 | プルダウン | ○ | 20:30 / 20:40 / 20:50 / 21:00 / 21:10 / 21:20 / 21:30 / 21:40 / 21:50 / 22:00 |
| 12 | 朝4時チェック | プルダウン | ○ | 睡眠 / 覚醒確認後に付き添い |
| 13 | 起床時刻 | プルダウン | ○ | 6:00 / 6:10 / 6:20 / 6:30 / 6:40 / 6:50 / 7:00 / 7:10 / 7:20 / 7:30 |
| 14 | 便 | プルダウン | × | ○ / × / △ / - |
| 15 | 服薬(夜) | プルダウン | × | ○ / × / △ / - |
| 16 | 服薬(朝) | プルダウン | × | ○ / × / △ / - |
| 17 | その他連絡事項 | 段落 | × | 自由記述 |

> **記録日廃止について（2026-04 後半）**: フォーム本体から「記録日」質問を削除し、フォーム送信時のタイムスタンプ（送信日時）の日付部分を「利用日」として扱う運用に変更。スタッフ用_回答シートの2列目「記録日」列は互換のため残存し、新規データでは空欄になる。読取時は `getRowRecordDate_()` ヘルパーで「RECORD_DATE 列があればそれ、空ならタイムスタンプ日付」にフォールバックする。

> **Googleフォーム本体への質問追加**: `syncFormDropdowns()` 実行時に「退所日時」→「退所予定日時」へのリネームは自動適用される（`form-sync.gs:ensureOvernightSchema_`）。

---

## 回答シートの列定義

フォーム送信後、スタッフ用_回答シートに以下の列構成で蓄積される（`FORM_COL`定数に対応、`gas/utils.gs`）。

| 列 | 列名 | GAS定数 | GAS定数インデックス | 備考 |
|----|------|--------|---------------|------|
| A | タイムスタンプ | TIMESTAMP | 1 | フォーム送信時刻（自動）。**この日付（送信日）を「利用日」として扱う** |
| B | 記録日 | RECORD_DATE | 2 | フォーム入力は廃止（互換のため列のみ残存・新規データは空欄）。読取時は `getRowRecordDate_()` で TIMESTAMP の日付にフォールバック |
| C | スタッフ1 | STAFF_NAME | 3 | |
| D | スタッフ2 | STAFF_NAME_2 | 4 | 任意 |
| E | 児童名 | CHILD_NAME | 5 | プルダウン選択 |
| F | 入所日時 | CHECK_IN | 6 | 入所日+時刻 |
| G | 退所予定日時 | CHECK_OUT | 7 | 退所日+時刻 |
| H | 体温 | TEMPERATURE | 8 | |
| I | 夕食 | MEAL_DINNER | 9 | ○ / × / 完食 / 半分 等 |
| J | 朝食 | MEAL_BREAKFAST | 10 | ○ / × / 完食 / 半分 等 |
| K | 昼食 | MEAL_LUNCH | 11 | 他サービス併給時は「-」 |
| L | 入浴 | BATH | 12 | ○ / × |
| M | 入眠時刻 | SLEEP_ONSET | 13 | 20:30〜22:00（10分刻み） |
| N | 朝4時チェック | SLEEP_CHECK_4AM | 14 | 睡眠 / 覚醒確認後に付き添い |
| O | 起床時刻 | WAKE_UP | 15 | 6:00〜7:30（10分刻み） |
| P | 便 | BOWEL | 16 | ○ / × |
| Q | 服薬(夜) | MEDICINE_NIGHT | 17 | ○ / × |
| R | 服薬(朝) | MEDICINE_MORNING | 18 | ○ / × |
| S | その他連絡事項 | NOTES | 19 | |
| T | メール送信済 | EMAIL_SENT | 20 | 初回送信時に `email.gs` がヘッダーと行値を書き込む（初期セットアップ対象外） |

---

## 宿泊レコード設計（フォーム1行=1宿泊方式）

**フォーム1行で入所〜退所までが完結する**運用。1回の宿泊（単泊・複数日宿泊いずれも）につき1レコードを送信し、入所日時・退所予定日時の両方を必ず記入する。
※ 同一児童が同日に2回別々の来館をすることは無い運用前提。

| 項目 | 単泊 | 複数日宿泊 |
|------|------|-----------|
| 入所日時 | 必須 | 必須（入所日+時刻） |
| 退所予定日時 | 必須 | 必須（退所日+時刻） |
| 様子・体温・食事等 | 当日分を記録 | 全期間を代表する記録 |
| 利用日（=タイムスタンプの日付） | 送信日 | 送信日 |

### 確定来館記録への展開

確定来館記録への書き込み時、フォーム1行を入所日〜退所予定日の日付範囲に展開し、各日付ごとに1行を生成する:
- 単泊（同日入退所） → 1行
- 1泊2日 → 2行、2泊3日 → 3行、3泊4日 → 4行

月別集計・年間利用枠のカウントは、対象月内に含まれる利用日（展開後の日付）のユニーク件数で行う。月またぎ・年またぎは入所日〜退所予定日の各日付が対象月（または年）に含まれるかでフィルタする。

### フォーム回答検証

`form-sync.gs` の検証関数で、入退所日時の欠落・時系列逆転（退所予定日時 < 入所日時）・複数レコード間での期間重複をチェックする。

### メール送信

自動送信(`sendVisitReportsRecent_`)は **「過去24時間以内に送信されたフォーム回答（タイムスタンプ基準）のうち、未送信(`EMAIL_SENT` 列が空)」** を対象とする。フォーム1行=1宿泊で入退所日時が完結しているため、メール本文はレコード単体の値をそのまま表示する。本文中の対象日付は `getRowRecordDate_()` の結果（利用日）を使う。
手動送信(`sendVisitReportsManual`)は HTML ダイアログで日付を指定し、**指定日のタイムスタンプ日付（送信日）**が一致するレコードを対象に送信する。

> **注意**:
> - 列Dのスタッフ2は要件定義書（旧版）に記載がなかったが、実装では存在する。
> - 2026-04以前の回答データは旧列構成(I:食事, M:服薬)のままで、新スキーマに移行するには旧値を対応列(夕食 or 服薬(夜))へコピーするマイグレーションが必要。本番運用に適用する前に実施すること。

---

## プルダウン同期の仕組み

### 方式1: フォームリンク経由（syncFormDropdowns）

`form-sync.gs` の `syncFormDropdowns()` が実行される。

```
スタッフマスタ（B列全員）→ スタッフ1 / スタッフ2 のプルダウン
児童マスタ（B列、稼働/休止のみ）→ 児童名プルダウン
```

- フォームのURLは `SpreadsheetApp.getActiveSpreadsheet().getFormUrl()` で取得（メインシートにフォームがリンクされている前提）
- 質問タイトルで一致照合: `FORM_TITLE_STAFF1 = 'スタッフ1'`, `FORM_TITLE_STAFF2 = 'スタッフ2'`, `FORM_TITLE_CHILD = '児童名'`

### 方式2: スクリプトプロパティ経由（updateFormChildNameDropdown_）

`refreshDropdowns()` 呼び出し時に使用。児童名のみ更新する。

- スクリプトプロパティ `FORM_ID`: フォームのID（必須設定）
- スクリプトプロパティ `FORM_CHILD_NAME_QUESTION`: 質問タイトル（デフォルト `'児童名'`）

---

## トリガー設定

| トリガー種別 | 実行タイミング | 関数名 | 設定方法 |
|------------|-------------|-------|---------|
| 時間ベース | 毎日 AM1時 | `syncFormDropdowns` | `setupFormSyncTrigger()` を1回手動実行して設定 |
| 手動（メニュー） | 任意 | `refreshDropdowns` | 「来館管理」メニュー → 「ドロップダウンを更新」 |

---

## スクリプトプロパティ一覧

| プロパティキー | 用途 | 必須/任意 |
|--------------|------|---------|
| `FORM_ID` | GoogleフォームのID（方式2の同期で使用） | 任意（未設定時はスキップ） |
| `FORM_CHILD_NAME_QUESTION` | 児童名質問のタイトル（デフォルト: 児童名） | 任意 |
| `ERROR_NOTIFY_EMAILS` | エラー通知先メール（カンマ区切り複数可） | 任意 |

---

## 連携スプレッドシート

| スプレッドシート | 用途 | ID定数 |
|--------------|------|--------|
| スタッフ用_回答シート | フォーム回答の蓄積・Webビュー編集先 | `STAFF_SHEET_ID`（utils.gs） |
| メイン管理シート | IMPORTRANGEで「フォームの回答」シートに参照 | — |
