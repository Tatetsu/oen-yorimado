# Googleフォーム 設計書

## 概要

スタッフが来館記録を入力するためのGoogleフォーム。回答データはスタッフ用_回答シート（別スプレッドシート）に蓄積され、メインシートにIMPORTRANGEで参照される。

---

## フォームの質問項目

2026-04 の仕様変更で、食事と服薬は行政様式(実績記録票)に合わせて分離されている。さらに 2026-04（後半）の仕様変更で、旧「睡眠」(○/×) を **入眠時刻 / 朝4時チェック / 起床時刻** の3項目（いずれもプルダウン・必須）に置換した。後続項目（便・服薬・連絡事項など）はフォーム上+2列ずれる。

| 順番 | 質問タイトル | 種別 | 必須 | 選択肢・検証 |
|------|------------|------|------|------------|
| 1 | スタッフ1 | プルダウン | ○ | スタッフマスタ B列から自動同期 |
| 2 | スタッフ2 | プルダウン | × | スタッフマスタ B列から自動同期 |
| 3 | 記録日 | 日付 | ○ | 手入力（カレンダーピッカー） |
| 4 | 児童名 | プルダウン | ○ | 児童マスタ B列（稼働・休止のみ）から自動同期 |
| 5 | 入所日時 | 日時 | × | 手入力（連泊・最終日は空欄） |
| 6 | 退所予定日時 | 日時 | × | 手入力（連泊・初日/中日は空欄） |
| 7 | 体温 | 短答 | × | 数値（例: 36.5） |
| 8 | 夕食 | プルダウン | × | ○ / × / △ / 完食 / 半分 / - |
| 9 | 朝食 | プルダウン | × | ○ / × / △ / 完食 / 半分 / - |
| 10 | 昼食 | プルダウン | × | - / ○ / × / △ / 完食 / 半分（他サービス併給時は「-」） |
| 11 | 入浴 | プルダウン | × | ○ / × / △ / - |
| 12 | 入眠時刻 | プルダウン | ○ | 20:30 / 20:40 / 20:50 / 21:00 / 21:10 / 21:20 / 21:30 / 21:40 / 21:50 / 22:00 |
| 13 | 朝4時チェック | プルダウン | ○ | 睡眠 / 覚醒確認後に付き添い |
| 14 | 起床時刻 | プルダウン | ○ | 6:00 / 6:10 / 6:20 / 6:30 / 6:40 / 6:50 / 7:00 / 7:10 / 7:20 / 7:30 |
| 15 | 便 | プルダウン | × | ○ / × / △ / - |
| 16 | 服薬(夜) | プルダウン | × | ○ / × / △ / - |
| 17 | 服薬(朝) | プルダウン | × | ○ / × / △ / - |
| 18 | その他連絡事項 | 段落 | × | 自由記述 |

> **Googleフォーム本体への質問追加**: `syncFormDropdowns()` 実行時に「退所日時」→「退所予定日時」へのリネームは自動適用される（`form-sync.gs:ensureOvernightSchema_`）。連泊フラグの追加は `addCheckboxItem()` で末尾に追加されるため、新規セットアップ時はその他連絡事項の直後に配置される。

---

## 回答シートの列定義

フォーム送信後、スタッフ用_回答シートに以下の列構成で蓄積される（`FORM_COL`定数に対応、`gas/utils.gs`）。

| 列 | 列名 | GAS定数 | GAS定数インデックス | 備考 |
|----|------|--------|---------------|------|
| A | タイムスタンプ | TIMESTAMP | 1 | フォーム送信時刻（自動） |
| B | 記録日 | RECORD_DATE | 2 | スタッフ入力 |
| C | スタッフ1 | STAFF_NAME | 3 | |
| D | スタッフ2 | STAFF_NAME_2 | 4 | 任意 |
| E | 児童名 | CHILD_NAME | 5 | プルダウン選択 |
| F | 入所日時 | CHECK_IN | 6 | 連泊・最終日は空欄 |
| G | 退所予定日時 | CHECK_OUT | 7 | 連泊・初日/中日は空欄 |
| H | 体温 | TEMPERATURE | 8 | |
| I | 夕食 | MEAL_DINNER | 9 | ○ / × / 完食 / 半分 等 |
| J | 朝食 | MEAL_BREAKFAST | 10 | ○ / × / 完食 / 半分 等 |
| K | 昼食 | MEAL_LUNCH | 11 | 他サービス併給時は「-」 |
| L | 入浴 | BATH | 12 | ○ / × |
| M | 入眠時刻 | SLEEP_ONSET | 13 | 20:30〜22:00（10分刻み） |
| N | 朝4時チェック | SLEEP_CHECK_4AM | 14 | 睡眠 / 覚醒確認後に付き添い |
| O | 起床時刻 | WAKE_UP | 15 | 6:00〜7:30（10分刻み） |
| P | 便 | BOWEL | 16 | ○ / × |
| Q | 服薬(夜) | MEDICINE_NIGHT | 17 | ○ / × |
| R | 服薬(朝) | MEDICINE_MORNING | 18 | ○ / × |
| S | その他連絡事項 | NOTES | 19 | |
| T | メール送信済 | EMAIL_SENT | 20 | 初回送信時に `email.gs` がヘッダーと行値を書き込む（初期セットアップ対象外） |

> **列順変更（2026-04）**: 連泊フラグを末尾(その他連絡事項の直後)に配置し、確定来館記録の2列目以降と並びを一致させた。既存データは手元で並び替え済み。新規セットアップでは `addCheckboxItem()` が末尾に追加するためそのまま揃う。

---

## 連泊レコード設計（2026-04 改訂: ユニーク宿泊キー方式）

連泊は1夜ごとに1レコードを送信する方式。**全レコードに入所日時・退所日時を必ず両方記入する**。
児童名+入所日（日付のみ、yyyy-MM-dd）を「宿泊PK」として論理1宿泊にペアリングする。連泊フラグ（チェックボックス）は廃止。
※ 同一児童が同日に2回別々の来館をすることは無い運用前提。連泊は1夜ごとに別レコードのため入所日が日ごとに異なり、PK衝突しない。

| 項目 | 単泊 | 連泊・初日 | 連泊・中日 | 連泊・最終日 |
|------|------|-----------|-----------|-------------|
| 入所日時 | 必須 | 必須 | 必須（同じ値） | 必須（同じ値） |
| 退所予定日時 | 必須 | 必須（同じ値） | 必須（同じ値） | 必須 |
| 様子・体温・食事等 | 必須 | 必須（その夜分） | 必須（その夜分） | 必須（最終朝の様子） |
| 記録日 | 翌朝（送信日） | 翌朝 | 翌朝 | 当日朝（最終日朝） |

### ペアリングロジック（`utils.gs:pairStayRecords_`）

`児童名 + 入所日（yyyy-MM-dd）` をキーに groupBy。同一キーのレコードは中日の様子記録として1宿泊に統合される。
- 状態機械を使わない（ON/OFFや空欄パターンに依存しない）
- プライマリ行 = 「記録日 == 入所日」となる行（無ければグループ内の記録日が最も早い行）

### カウントへの影響

ペアリング後、`expandStayToDates_(checkIn, checkOut)` で滞在カレンダー全日を展開してカウント:
- 1泊2日 → 2カウント、2泊3日 → 3カウント、3泊4日 → 4カウント

月またぎ・年またぎは入所日〜退所日の各日付が対象月（または年）に含まれるかでフィルタする。

### メール送信（B-1: 連泊中も毎朝）

`email.gs:getFormResponsesByDate_` は **「記録日」基準**でフィルタ。連泊中の各レコードを毎朝8時に1通ずつ送信する。
新仕様では全レコードに入退所日時が記入されているため、メール本文には各レコードの値をそのまま表示する。

### 旧仕様（空欄パターン方式）からの移行

`gas/migration.gs:migrateLegacyOvernightRecords` を手動実行することで、旧データの空欄を新仕様に変換できる。
- メニュー実行 or エディタから直接実行
- ドライランは `migrateLegacyOvernightRecordsDryRun`
- トリガー禁止

> **注意**:
> - 列Dのスタッフ2は要件定義書（旧版）に記載がなかったが、実装では存在する。
> - 2026-04以前の回答データは旧列構成(I:食事, M:服薬)のままで、新スキーマに移行するには旧値を対応列(夕食 or 服薬(夜))へコピーするマイグレーションが必要。本番運用に適用する前に実施すること。

---

## プルダウン同期の仕組み

### 方式1: フォームリンク経由（syncFormDropdowns）

`form-sync.gs` の `syncFormDropdowns()` が実行される。

```
スタッフマスタ（B列全員）→ スタッフ1 / スタッフ2 のプルダウン
児童マスタ（B列、稼働/休止のみ）→ 児童名プルダウン
```

- フォームのURLは `SpreadsheetApp.getActiveSpreadsheet().getFormUrl()` で取得（メインシートにフォームがリンクされている前提）
- 質問タイトルで一致照合: `FORM_TITLE_STAFF1 = 'スタッフ1'`, `FORM_TITLE_STAFF2 = 'スタッフ2'`, `FORM_TITLE_CHILD = '児童名'`

### 方式2: スクリプトプロパティ経由（updateFormChildNameDropdown_）

`refreshDropdowns()` 呼び出し時に使用。児童名のみ更新する。

- スクリプトプロパティ `FORM_ID`: フォームのID（必須設定）
- スクリプトプロパティ `FORM_CHILD_NAME_QUESTION`: 質問タイトル（デフォルト `'児童名'`）

---

## トリガー設定

| トリガー種別 | 実行タイミング | 関数名 | 設定方法 |
|------------|-------------|-------|---------|
| 時間ベース | 毎日 AM1時 | `syncFormDropdowns` | `setupFormSyncTrigger()` を1回手動実行して設定 |
| 手動（メニュー） | 任意 | `refreshDropdowns` | 「来館管理」メニュー → 「ドロップダウンを更新」 |

---

## スクリプトプロパティ一覧

| プロパティキー | 用途 | 必須/任意 |
|--------------|------|---------|
| `FORM_ID` | GoogleフォームのID（方式2の同期で使用） | 任意（未設定時はスキップ） |
| `FORM_CHILD_NAME_QUESTION` | 児童名質問のタイトル（デフォルト: 児童名） | 任意 |
| `ERROR_NOTIFY_EMAILS` | エラー通知先メール（カンマ区切り複数可） | 任意 |

---

## 連携スプレッドシート

| スプレッドシート | 用途 | ID定数 |
|--------------|------|--------|
| スタッフ用_回答シート | フォーム回答の蓄積・Webビュー編集先 | `STAFF_SHEET_ID`（utils.gs） |
| メイン管理シート | IMPORTRANGEで「フォームの回答」シートに参照 | — |
